package com.marksman.controller;

/**
 * В ЛР2 экран окончания игры не используется.
 * Победитель объявляется в GameViewController через statusLabel
 * при получении сообщения GAME_OVER от сервера.
 */
public class GameOverController {
    // Оставлен пустым для совместимости со сборкой.
    // gameOverView.fxml можно не удалять, он просто не открывается.
}